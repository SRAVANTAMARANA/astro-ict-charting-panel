version: "3"
services:
  backend:
    build: ./backend
    container_name: ict-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: ict-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
