services:
  backend:
    build: ./Backend
    container_name: astro-ict-backend
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    build: ./Frontend
    container_name: astro-ict-frontend
    ports:
      - "3000:8080"
    restart: unless-stopped
