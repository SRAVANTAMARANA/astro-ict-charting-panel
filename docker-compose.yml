services:
  frontend:
    build: ./frontend   # or image: ...
    ports:
      - "3000:80"
    volumes:
      - ./frontend/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./frontend/static:/usr/share/nginx/html:ro   # if you serve static files
    depends_on:
      - backend
